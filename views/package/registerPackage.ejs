

<div class="container">
    <br />
    <form action='/package' method='post'>
        <% function compare( a, b ) {
            if ( a.name.toLowerCase() < b.name.toLowerCase() ){
              return -1;
            }
            if ( a.name.toLowerCase() > b.name.toLowerCase() ){
              return 1;
            }
            return 0;
        } %>
        <% citiesArray.sort(compare) %>
        <div class="form-group">
            <label for="weight">Weight (Kgs)</label>
            <input name="weight" onkeyup="calculateAmount();" type="number" class="form-control" id="weight" required>
        </div>
        <div class="form-group">
            <label for="source_city_id">Source City</label>

            <select class="form-control" name="source_city_id" id="source_city_id" required>
                <% for(let city of citiesArray) { %>
                <option value=<%= `${city.id}` %>><%= `${city.name}` %></option>
                <% } %> </select>
        </div>

        <div class="form-group">
            <label for="destination_city_id">Destination City</label>

            <select class="form-control" name="destination_city_id" id="destination_city_id" required>
                <% for(let city of citiesArray) { %>
                <option value=<%= `${city.id}` %>><%= `${city.name}` %></option>
                <% } %>
            </select>
        </div>

        <div class="form-group">
            <label for="name">Receiver Name</label>
            <input name="name" type="text" class="form-control" id="name" required>
        </div>
        <div class="form-group">
            <label for="email">Receiver Email</label>
            <input name="email" type="email" class="form-control" id="email" required>
        </div>
        <div class="form-group">
            <label for="phoneNo">Receiver Phone</label>
            <input name="phoneNo" type="text" class="form-control" id="phoneNo" pattern=".{10}"
                title="10 characters only" required>
        </div>
        <div class="form-group">
            <label for="phoneNo">Amount To be Paid</label>
            <input name="amount" type="text" class="form-control" id="amount" readonly>
        </div>
        <button type="submit" value="Submit" class="btn btn-primary">Pay</button>
    </form>
</div>

<script src="/javascripts/package.js"></script>